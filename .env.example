# === Монорепо «Грузовые ЭПЛ» ===
#
# Переменные приложения задаются в пакетах:
#   - backend/.env.example   → скопировать в backend/.env
#   - frontend/.env.example  → скопировать в frontend/.env.development (или задать в CI)
#
# Блок ниже — опционально для скриптов деплоя / Netlify (если используете).

# Netlify (автообновление переменных и передеплой)
# NETLIFY_SITE_ID=
# NETLIFY_API_TOKEN=
