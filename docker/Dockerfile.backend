# https://hub.docker.com/_/node — образ node:24-slim
# sqlite3 (нативный модуль) требует компиляторы в slim-образе
FROM node:24-slim

RUN apt-get update \
  && apt-get install -y --no-install-recommends python3 make g++ ca-certificates \
  && rm -rf /var/lib/apt/lists/*

WORKDIR /app
